{{#each fonts}}
@font-face
  font-family '{{fontName}}'
  font-display swap
  src url('/src/fonts/{{originName}}.woff'), url('/src/fonts/{{originName}}.woff2')
{{#unless @last}}

{{/unless}}
{{/each}}
